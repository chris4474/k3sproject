labels:
- pairs:
    description: "Hello"

secretGenerator:
- name: apps-tls-secret
  files:
  - secrets/tls.crt
  - secrets/tls.key
  type: "kubernetes.io/tls"

namespace: hello-app

resources:
- namespace.yaml
- ingress.yaml
- workload.yaml
